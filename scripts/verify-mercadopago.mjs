import{readFileSync}from"node:fs";
const sql=readFileSync("supabase/migrations/202608050003_vertex_support_payments.sql","utf8"),orders=readFileSync("supabase/functions/mercadopago-orders/index.ts","utf8"),webhook=readFileSync("supabase/functions/mercadopago-webhook/index.ts","utf8"),ui=readFileSync("src/pages/SupportVertex.tsx","utf8"),env=readFileSync(".env.example","utf8");
const checks={"Valor mínimo e máximo server-side":/MIN=5,MAX=1000/.test(orders)&&/amount between 5 and 1000/.test(sql),"Usuário deriva da sessão":/auth\.getUser\(\)/.test(orders)&&/user_id:user\.id/.test(orders),"Idempotência dupla":/X-Idempotency-Key/.test(orders)&&/unique\(user_id,client_request_id\)/.test(sql),"Orders API atual":/api\.mercadopago\.com\/v1\/orders/.test(orders),"Webhook HMAC e confirmação remota":/x-signature/.test(webhook)&&/subtle\.sign/.test(webhook)&&/v1\/orders/.test(webhook),"Teste não concede badge":/environment='production'/.test(sql)&&/live_mode=false/.test(sql),"Cliente não altera status":/revoke all on public\.support_payments/.test(sql)&&!/grant (insert|update|delete)/.test(sql),"Nenhum dado PCI persistido":!/card_number|security_code|cvv/i.test(sql),"UI bloqueia submit e valida valor":/if\(busy\)return/.test(ui)&&/chosen>=MIN&&chosen<=MAX/.test(ui),"Pix, crédito, débito e 3DS/status":/bankTransfer:\["pix"\]/.test(ui)&&/creditCard:"all"/.test(ui)&&/debitCard:"all"/.test(ui)&&/pending_challenge/.test(ui),"Secrets separados":/VITE_MERCADO_PAGO_PUBLIC_KEY/.test(env)&&/MERCADO_PAGO_ACCESS_TOKEN/.test(env)&&/MERCADO_PAGO_WEBHOOK_SECRET/.test(env)};
for(const[n,ok]of Object.entries(checks))console.log(`${ok?"PASS":"FAIL"} ${n}`);if(Object.values(checks).some(x=>!x))process.exit(1);
