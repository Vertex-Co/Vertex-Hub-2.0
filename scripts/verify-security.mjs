import {readFileSync} from "node:fs";
const migration=readFileSync("supabase/migrations/202608050001_security_hardening_mfa.sql","utf8"),edge=readFileSync("supabase/functions/user-admin/index.ts","utf8"),gate=readFileSync("src/components/auth/MfaGate.tsx","utf8");
const checks={"Super Admin exige AAL2 no banco":/jwt\(\).*aal[\s\S]*aal2/.test(migration),"Identidade deriva de auth.uid":/m\.user_id=auth\.uid\(\)/.test(migration),"Campos de autoridade protegidos":/protect_profile_security_fields/.test(migration),"Membership sem escrita REST":/revoke insert,update,delete on public\.company_members/.test(migration),"Anon sem helpers privilegiados":/from public,anon/.test(migration),"Edge Function exige AAL2":/hasSuperRole&&aal==="aal2"/.test(edge),"TOTP oficial":/auth\.mfa\.enroll/.test(gate)&&/auth\.mfa\.verify/.test(gate)};
for(const[name,ok]of Object.entries(checks))console.log(`${ok?"PASS":"FAIL"} ${name}`);if(Object.values(checks).some(x=>!x))process.exit(1);
