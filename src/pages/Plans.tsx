import { CheckCircle2,ExternalLink } from "lucide-react";
import { Button,Card } from "../components/ui/Common";
import { plans,whatsappForPlan } from "../config/plans";
import { useCompany } from "../contexts/CompanyContext";
export function Plans(){const{activeCompany}=useCompany();return <><div className="mb-6"><p className="text-sm text-zinc-500">Planos opcionais</p><h2 className="text-3xl font-black">Planos Vertex Hub</h2><p className="mt-3 max-w-3xl text-sm leading-relaxed text-zinc-500">O Vertex Hub pode ser utilizado gratuitamente. Os planos pagos são opcionais e atendem empresas que precisam de mais usuários e estrutura. Você pode continuar no Free pelo tempo que desejar.</p></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-5">{plans.map(plan=>{const current=(activeCompany?.plan??"free")===plan.id;return <Card key={plan.id} className={`flex flex-col ${current?"border-blue-500 ring-2 ring-blue-500/10":""}`}><div className="flex items-center justify-between"><b className="text-sm uppercase tracking-wider text-blue-500">{plan.name}</b>{current&&<span className="rounded-full bg-blue-500/10 px-2 py-1 text-[10px] font-bold text-blue-500">PLANO ATUAL</span>}</div><p className="mt-4 text-2xl font-black">{plan.price}</p><p className="mt-1 text-sm font-semibold">{plan.users}</p><p className="mt-4 text-sm text-zinc-500">{plan.description}</p><ul className="my-5 flex-1 space-y-2">{plan.features.map(f=><li key={f} className="flex gap-2 text-xs text-zinc-500"><CheckCircle2 size={15} className="shrink-0 text-emerald-500"/>{f}</li>)}</ul>{plan.id==="free"?<p className="text-center text-xs font-semibold text-zinc-500">Sempre disponível</p>:<a href={whatsappForPlan(plan.name)} target="_blank" rel="noreferrer"><Button className="w-full">Falar com a Vertex<ExternalLink size={15}/></Button></a>}</Card>})}</div></>}
