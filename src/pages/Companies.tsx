import { Building2,ExternalLink,Search,ShieldAlert } from "lucide-react";import { useMemo,useState } from "react";import { Button,Card,Input,Select } from "../components/ui/Common";import { useCompany } from "../contexts/CompanyContext";import { supabase } from "../services/supabase";import type { Company } from "../types";import { formatCNPJ,formatPhone } from "../utils/brDocuments";
export function Companies({onOpenDashboard}:{onOpenDashboard:()=>void}){const{companies,activeCompany,selectCompany,isSuperAdmin,reload}=useCompany();const[query,setQuery]=useState(""),[status,setStatus]=useState("all"),[busy,setBusy]=useState(false);const filtered=useMemo(()=>companies.filter(c=>`${c.name} ${c.cnpj??""}`.toLowerCase().includes(query.toLowerCase())&&(status==="all"||c.status===status)),[companies,query,status]);const toggle=async(c:Company)=>{setBusy(true);await supabase.rpc("admin_set_company_status",{p_company_id:c.id,p_status:c.status==="suspended"?"active":"suspended"});await reload();setBusy(false)};return <><div className="mb-6"><p className="text-sm text-zinc-500">Gestão de empresas</p><h2 className="text-3xl font-black">Empresas</h2></div><div className="mb-5 grid gap-3 md:grid-cols-[1fr_180px]"><div className="relative"><Search className="absolute left-3 top-3 text-zinc-400" size={18}/><Input className="pl-10" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar por nome ou CNPJ..."/></div><Select value={status} onChange={e=>setStatus(e.target.value)}><option value="all">Todos os status</option><option value="active">Ativas</option><option value="suspended">Suspensas</option></Select></div><div className="grid gap-4 xl:grid-cols-2">{filtered.map(c=><Card key={c.id} className={activeCompany?.id===c.id?"border-blue-500/60":""}><div className="flex items-start gap-4"><span className="grid size-12 place-items-center rounded-xl bg-blue-500/10 text-blue-500"><Building2/></span><div className="min-w-0 flex-1"><h3 className="font-bold">{c.name}</h3><p className="text-sm text-zinc-500">{c.cnpj?formatCNPJ(c.cnpj):"Sem CNPJ"}</p><p className="mt-2 text-xs text-zinc-500">{c.email??"E-mail não informado"} • {c.phone?formatPhone(c.phone):"Telefone não informado"}</p></div><span className={`text-xs font-bold ${c.status==="active"?"text-emerald-500":"text-red-500"}`}>{c.status==="active"?"Ativa":"Suspensa"}</span></div><div className="mt-5 flex flex-wrap gap-2"><Button onClick={()=>{selectCompany(c.id);onOpenDashboard()}}><ExternalLink size={17}/>Visualizar empresa</Button>{isSuperAdmin&&<Button variant={c.status==="suspended"?"secondary":"danger"} disabled={busy} onClick={()=>void toggle(c)}><ShieldAlert size={16}/>{c.status==="suspended"?"Reativar":"Suspender"}</Button>}</div></Card>)}</div>{!filtered.length&&<Card className="py-14 text-center text-zinc-500">Nenhuma empresa encontrada.</Card>}</>}
