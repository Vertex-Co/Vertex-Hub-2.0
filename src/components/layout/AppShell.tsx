import { BarChart3,Building2,CircleDot,Eye,EyeOff,Flag,LayoutDashboard,LogOut,Menu,PiggyBank,Plus,ReceiptText,Settings,X } from "lucide-react";
import { useState } from "react";
import { useAuth } from "../../contexts/AuthContext";
import { useCompany } from "../../contexts/CompanyContext";
import { useFinance } from "../../contexts/FinanceContext";
import type { Page } from "../../types";
import { Button } from "../ui/Common";
import { AppFooter } from "./AppFooter";
const nav:[Page,string,React.ElementType][]=[["dashboard","Visão geral",LayoutDashboard],["transactions","Transações",ReceiptText],["reports","Relatórios",BarChart3],["budgets","Orçamentos",PiggyBank],["goals","Metas",Flag],["settings","Configurações",Settings]];
export function AppShell({page,setPage,onNew,children}:{page:Page;setPage:(p:Page)=>void;onNew:()=>void;children:React.ReactNode}){const{user,signOut}=useAuth(),{activeCompany,companies,isAdmin,profile,selectCompany}=useCompany(),{hiddenValues,setHiddenValues,toasts,loading}=useFinance();const[mobile,setMobile]=useState(false);const go=(p:Page)=>{setPage(p);setMobile(false)};const name=profile?.fullName||String(user?.user_metadata.full_name??user?.email?.split("@")[0]??"Usuário");return <div className="min-h-screen bg-zinc-100 text-zinc-900 dark:bg-zinc-950 dark:text-zinc-50"><aside className={`fixed inset-y-0 left-0 z-40 w-64 border-r border-zinc-200 bg-white p-5 transition-transform dark:border-zinc-800 dark:bg-zinc-950 lg:translate-x-0 ${mobile?"translate-x-0":"-translate-x-full"}`}><div className="mb-8 flex items-center justify-between"><button onClick={()=>go("dashboard")} className="flex items-center gap-3 text-xl font-black"><span className="grid size-10 place-items-center rounded-xl bg-gradient-to-br from-blue-600 to-violet-600 text-white"><CircleDot/></span><span className="text-left">Vertex Hub<small className="text-[10px] font-medium text-zinc-500">Central de Gestão Digital</small></span></button><button className="lg:hidden" onClick={()=>setMobile(false)}><X/></button></div><nav className="space-y-1">{nav.map(([id,label,Icon])=><button key={id} onClick={()=>go(id)} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-sm font-medium ${page===id?"bg-blue-600 text-white":"text-zinc-500 hover:bg-zinc-100 dark:hover:bg-zinc-900"}`}><Icon size={19}/>{label}</button>)}</nav>{isAdmin&&<div className="mt-7 border-t pt-5 dark:border-zinc-800"><p className="mb-2 px-3 text-[10px] font-black tracking-widest text-zinc-500">GESTÃO VERTEX</p><button onClick={()=>go("companies")} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-sm font-medium ${page==="companies"?"bg-blue-600 text-white":"text-zinc-500 hover:bg-zinc-100 dark:hover:bg-zinc-900"}`}><Building2 size={19}/>Empresas</button></div>}<div className="absolute bottom-5 left-5 right-5 border-t pt-4 dark:border-zinc-800"><p className="truncate font-semibold">{isAdmin?"Vertex":activeCompany?.name}</p><p className="truncate text-xs text-zinc-500">{isAdmin?`${name} • ${profile?.globalRole==="super_admin"?"Super Administrador":"Administrador"}`:user?.email}</p><button onClick={()=>void signOut()} className="mt-3 flex items-center gap-2 text-sm text-zinc-500"><LogOut size={17}/>Sair</button></div></aside>{mobile&&<button className="fixed inset-0 z-30 bg-black/50 lg:hidden" onClick={()=>setMobile(false)}/>}<main className="min-h-screen lg:pl-64"><header className="sticky top-0 z-20 border-b bg-zinc-100/85 px-4 py-4 backdrop-blur-xl dark:border-zinc-800 dark:bg-zinc-950/85"><div className="mx-auto flex max-w-[1500px] items-center gap-3"><button className="rounded-xl border p-2 lg:hidden" onClick={()=>setMobile(true)}><Menu/></button><div className="mr-auto"><h1 className="font-bold">Olá, {name.split(" ")[0]}</h1><p className="text-xs text-zinc-500">{loading?"Sincronizando...":activeCompany?.name}</p></div><button onClick={()=>setHiddenValues(!hiddenValues)} className="rounded-xl border bg-white p-2.5 dark:border-zinc-700 dark:bg-zinc-900">{hiddenValues?<EyeOff size={19}/>:<Eye size={19}/>}</button><Button onClick={onNew}><Plus size={18}/><span className="hidden sm:inline">Nova transação</span></Button></div></header>{isAdmin&&<div className="border-b border-blue-500/20 bg-blue-500/10 px-4 py-2 text-sm"><div className="mx-auto flex max-w-[1500px] items-center justify-between gap-3"><span>Administrando: <b>{activeCompany?.name??"—"}</b></span><select value={activeCompany?.id??""} onChange={e=>selectCompany(e.target.value)} className="max-w-56 rounded-lg border bg-white px-3 py-1.5 dark:bg-zinc-900">{companies.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div></div>}<div className="mx-auto max-w-[1500px] p-4 pb-24 sm:p-7">{children}<AppFooter/></div></main><div className="fixed right-4 top-20 z-[60] space-y-2">{toasts.map(t=><div key={t.id} className={`rounded-xl px-4 py-3 text-sm font-semibold text-white ${t.kind==="error"?"bg-red-500":"bg-emerald-500"}`}>{t.message}</div>)}</div></div>}
